#include <stdio.h>

bool 
int main(){
    int x, y;
    scanf("%d %d", &x, &y);
    for (int i = x; i <= y; i++)
    {
        if ( isPrime(i) )
        {
            if (  )
            {
                /* code */
            }
            
        }
        
    }
    
    return 0;
}